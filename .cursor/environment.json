{
  "install": "export PATH=\"$HOME/.bun/bin:$HOME/.local/bin:$PATH\" && command -v bun >/dev/null || curl -fsSL https://bun.sh/install | bash && bun install --frozen-lockfile",
  "terminals": [
    {
      "name": "terminal",
      "command": "export PATH=\"$HOME/.bun/bin:$PATH\" && bun run terminal",
      "description": "Browser terminal stack — HTML :4321, PTY broker :4322"
    }
  ],
  "ports": [
    { "name": "html", "port": 4321 },
    { "name": "pty", "port": 4322 }
  ]
}
